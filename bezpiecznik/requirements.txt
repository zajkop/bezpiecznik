# Bezpiecznik — core dependencies (everything else is the standard library)
httpx>=0.27
pydantic>=2.0

# SPA-recon (headless browser) — optional, degrades gracefully when absent:
#   pip install playwright && python -m playwright install chromium
playwright>=1.40

# Optional external tools (installed separately; wrappers degrade gracefully when absent):
#   pip install garak pyrit promptfoo   # AI red team
#   brew install nmap sqlmap nikto ; go install .../ffuf ; nuclei  # web/recon
