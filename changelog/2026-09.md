# Changelog 2026-09

## 2026-09-01

Daily maintainer run — new web-side detector for a real, in-the-wild AI-attack gap.

- **Researched:** *Hidden / concealed indirect prompt injection served in web-page content* — a
  page carries an AI-directed instruction aimed not at the human reader but at any downstream LLM
  agent, crawler, RAG pipeline or "summarize this page" assistant that later ingests it. The
  instruction is concealed from a human via an HTML comment, a CSS/attribute-hidden element
  (`display:none`, `visibility:hidden`, `opacity:0`, `font-size:0`, off-screen positioning,
  `hidden`/`aria-hidden`) or zero-width / invisible Unicode. Palo Alto **Unit 42** documented this
  exact technique being used **in the wild** against AI agents in 2026, cataloguing the concealment
  methods (CSS zero-sizing/off-screen/transparency, zero-width chars, homoglyphs, HTML-comment and
  data-attribute hiding). Sources: Unit 42, "Fooling AI Agents: Web-Based Indirect Prompt Injection
  Observed in the Wild" (https://unit42.paloaltonetworks.com/ai-agent-prompt-injection/); OWASP
  Top 10 2026 context — the AI-consumed-content and supply-chain shift
  (https://cyberpress.org/owasp-releases-2026-top-10-list/).
- **Gap:** Bezpiecznik already tested the *offensive* indirect-injection side (feeding hidden-
  instruction payloads **to a chatbot** and checking it executes them — `indirect-fuzz.json` in the
  AI harness). It had **no** detector for the *defensive/recon* side: a web target whose own served
  HTML hosts concealed instructions that would hijack any AI agent browsing it (e.g. via
  unsanitized user-generated content). Grep confirmed nothing under `adapters/web/` or
  `adapters/recon/` inspected page content for hidden AI instructions.
- **Added** `ModernScanner._hidden_ai_injection()` + detection helpers/signatures in
  `bezpiecznik/adapters/web/modern_scanner.py`, wired into `ModernScanner.scan()` via a new
  `ai_inject_paths` param, and into `agent/orchestrator.py` (auto-run over the pages discovered by
  recon; consistent with how CORS/Host-header/GraphQL are wired — no separate agent tool needed).
  It GETs a page and fires **only** when an AI-directed imperative (`ignore previous instructions`,
  `system prompt`, `you are now`, `reveal/forward … secret/prompt`, `DAN/developer mode`, role-
  prefixed `assistant:/system:` commands, `reply only with …`) **co-occurs with a concealment
  mechanism** — instruction inside an HTML comment, inside a CSS/attribute-hidden element, or
  smuggled with zero-width Unicode (only surfaces once the invisible chars are stripped and is not
  already visible raw). This co-occurrence gate keeps false positives low. Maps to **LLM01:2025
  Prompt Injection / CWE-1427**, HIGH.
- **Tested:** `python3 -m compileall bezpiecznik` clean. Self-contained suite (unit + end-to-end
  with the **real** `ModernScanner` over a throwaway localhost HTTP server): 4 positives **fire** —
  comment-hidden, `display:none`, off-screen (`left:-9999px`), and zero-width-Unicode-smuggled
  instructions; 3 negatives stay **silent** — a clean page, a benign hidden element (tracking-pixel
  text, no instruction), and (the key false-positive guard) an article that *visibly* discusses
  "ignore previous instructions" and the "system prompt" with no concealment. A `scan()` smoke test
  confirmed the new detector routes through the public entry point. 4 fire / 3 silent, as expected.
- **Docs:** GUIDE.md — new Web/API coverage-matrix row ("Hidden indirect prompt injection in served
  content") and a cross-reference from the AI-matrix LLM01 indirect-injection row.

## 2026-09-02
- **Vector researched:** ChatInject — abusing model chat-template role delimiters for prompt
  injection in LLM agents. Attacker-controlled data (a served page, tool output, RAG document)
  carries a model's own turn-boundary tokens (ChatML `<|im_start|>system`, Llama-3
  `<|start_header_id|>assistant`, Llama-2 `<<SYS>>`, Gemma `<start_of_turn>`, `[INST]`, alpaca
  `### Instruction:`). An agent that naively concatenates untrusted content into its prompt template
  has the forged markers parsed as real turns, so the data escapes its sandbox and opens a spoofed
  system/assistant turn. Sources: arXiv:2509.22830 ("ChatInject: Abusing Chat Templates for Prompt
  Injection in LLM Agents"); OWASP LLM Top 10 2026 (prompt injection #1); Unit 42 in-the-wild
  indirect-injection reporting.
- **Gap:** the existing web-side detector flags AI instructions *concealed* from humans (comment /
  CSS-hidden / zero-width Unicode) but does **not** flag forged chat-template control tokens, which
  are the ChatInject attack itself and need no concealment. Real, valuable, deterministic, low-FP.
- **Added:** `bezpiecznik/adapters/web/modern_scanner.py` — `_match_chat_template()` +
  `_chat_template_injection()`, wired into the served-content (`ai_inject_paths`) loop so it reuses
  the already-fetched HTML (no extra request, no orchestrator change). High-specificity `<|…|>` /
  `<<SYS>>` / `<start_of_turn>` tokens fire on presence; soft markers (`[INST]`, `### Instruction:`)
  fire only when paired with a nearby system/assistant role switch, keeping false positives near
  zero. Maps to **LLM01:2025 Prompt Injection / CWE-1427**, HIGH.
- **Tested:** `python3 -m compileall bezpiecznik` clean. Unit test: 5 positives fire (ChatML,
  Llama-3, Llama-2, soft `[INST]`, soft alpaca), 5 benign negatives stay silent (marketing copy,
  "send a POST request" API instructions, prose mentioning "system"/"assistant", an installation
  guide, "contact system administrator"). End-to-end with the real `ModernScanner` over a localhost
  target: `/rag-doc` (a comment carrying `<|im_start|>system … <|im_end|>`) **fires**; a same-shape
  `/rag-doc-safe` with no tokens stays **silent**. 5/5 + fire/silent as expected.
- **Docs:** GUIDE.md — new Web/API coverage-matrix row ("Forged chat-template tokens in served
  content (ChatInject)") and a cross-reference from the AI-matrix LLM01 indirect-injection row.

## 2026-09-03
- **Vector researched:** *MCP tool poisoning* (OWASP MCP Top 10 **MCP03:2025**). A Model
  Context Protocol server advertises its tools via `tools/list`; each tool's free-text
  `description` and per-parameter schema descriptions are fed **verbatim** into a consuming
  agent's context. Tool poisoning plants adversarial instructions inside those descriptions —
  hijacking the agent the moment the tool list loads, **even if the poisoned tool is never
  called**, and typically concealed from the human who only sees a short summary. Tell-tale
  side channels: the Invariant Labs `<IMPORTANT>` wrapper, "do not tell the user" secrecy
  directives, "before using this tool …" preambles, and directives to read/exfiltrate local
  secrets (`~/.ssh/id_rsa`, `.env`, `~/.cursor/mcp.json`). Sources: OWASP MCP03:2025 Tool
  Poisoning (https://owasp.org/www-project-mcp-top-10/2025/MCP03-2025%E2%80%93Tool-Poisoning);
  Invariant Labs, "MCP Security Notification: Tool Poisoning Attacks"
  (https://invariantlabs.ai/blog/mcp-security-notification-tool-poisoning-attacks); MCPTox
  benchmark, arXiv:2508.14925.
- **Gap:** existing served-content detectors (`_hidden_ai_injection`, `_chat_template_injection`)
  only inspect `text/html` responses and bail on JSON, so an MCP JSON tool manifest was never
  examined. Grep confirmed no `mcp` / `tools/list` / `inputSchema` handling anywhere in the
  package. Distinct surface (tool metadata JSON, not page HTML), real, valuable, deterministic,
  low-FP.
- **Added:** `bezpiecznik/adapters/web/modern_scanner.py` — `_mcp_tool_poisoning()` plus helpers
  `_parse_tools()` (direct JSON **and** SSE `data:` framing), `_find_tools()`, `_walk_descriptions()`,
  `_collect_descriptions()`, `_mcp_signal()`, and the `_MCP_SIDECHANNEL` signature. POSTs a
  JSON-RPC `tools/list` (falls back to GET only for manifest-shaped paths, so it never hangs a GET
  on a live SSE stream), walks each tool's name/description/parameter descriptions, and fires when
  a signal co-occurs: an AI-directed imperative (reuses `_match_instruction`), a forged
  chat-template token (`_match_chat_template`), an MCP side-channel directive, or one smuggled with
  zero-width Unicode. New `mcp_paths` param on `ModernScanner.scan()` (default `/mcp`, `/messages`,
  `/sse`), wired into `agent/orchestrator.py` alongside the other modern classes (no new agent tool
  needed). Maps to **LLM01:2025 Prompt Injection / CWE-1427**, HIGH.
- **Tested:** `python3 -m compileall bezpiecznik` clean. Self-contained suite (unit + end-to-end
  with the **real** `ModernScanner` over a throwaway localhost server serving MCP `tools/list`):
  unit `_mcp_signal` 5/5 positives fire (`<IMPORTANT>` file-read, "ignore previous instructions",
  "do not tell the user", "before using this tool", forged `<|im_start|>`), 5/5 benign tool
  descriptions stay silent (weather, email-with-validation, recipient address, access-token note,
  "use markdown"); JSON + SSE `data:` parsing OK. End-to-end: a poisoned manifest **fires once**
  (`<IMPORTANT>` reading `~/.ssh/id_rsa`), a manifest with a **zero-width-smuggled** "do not tell
  the user" directive **fires once**, and a clean two-tool manifest stays **silent**. Non-MCP/404
  responses parse to zero tools → no finding, no crash.
- **Docs:** GUIDE.md — new Web/API coverage-matrix row ("MCP tool poisoning in a served tool
  manifest (MCP03:2025)") and a cross-reference from the AI-matrix LLM01 indirect-injection row.
